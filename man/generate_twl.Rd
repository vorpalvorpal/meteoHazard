% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_twl.R
\name{generate_twl}
\alias{generate_twl}
\title{Calculate Thermal Work Limit (TWL)}
\usage{
generate_twl(
  datetime,
  latitude,
  longitude,
  temp = NULL,
  wind_speed = NULL,
  RH = NULL,
  direct_solar = NULL,
  diffuse_solar = NULL,
  pressure = NULL,
  wet_bulb = NULL,
  albedo = 0.12,
  Icl = 0.6,
  icl = 0.45,
  max_core_temp = 38.2,
  max_sweat_rate = 0.67,
  convert_pressure = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{datetime}{POSIXct datetime vector (required).}

\item{latitude}{Latitude in decimal degrees (required).}

\item{longitude}{Longitude in decimal degrees (required).}

\item{temp}{Dry bulb air temperature in degrees Celsius, or \code{NULL} to
fetch from Open-Meteo.}

\item{wind_speed}{Wind speed in m/s, or \code{NULL} to fetch from Open-Meteo.}

\item{RH}{Relative humidity in percent, or \code{NULL} to fetch from
Open-Meteo.}

\item{direct_solar}{Direct beam solar radiation in W/m^2, or \code{NULL} to
fetch from Open-Meteo.}

\item{diffuse_solar}{Diffuse sky solar radiation in W/m^2, or \code{NULL} to
fetch from Open-Meteo.}

\item{pressure}{Barometric pressure in hPa (or kPa if \code{convert_pressure = FALSE}), or \code{NULL} to fetch from Open-Meteo.}

\item{wet_bulb}{Natural (unventilated) wet bulb temperature in degrees
Celsius, or \code{NULL} (the default) to estimate it from \code{temp}, \code{RH},
\code{pressure}, \code{wind_speed}, and \code{globe_temp}.}

\item{albedo}{Ground albedo (0--1). Default 0.12 for asphalt.}

\item{Icl}{Intrinsic clothing thermal resistance in clo. Default 0.6.}

\item{icl}{Clothing vapour permeation efficiency (0--1). Default 0.45.}

\item{max_core_temp}{Maximum acceptable core temperature in degrees Celsius.
Default 38.2.}

\item{max_sweat_rate}{Maximum acceptable sweat rate in kg/(m^2.hr).
Default 0.67.}

\item{convert_pressure}{If \code{TRUE} (the default), converts \strong{user-supplied}
pressure from hPa to kPa. API-fetched pressure (Open-Meteo
\code{surface_pressure}) is always converted hPa→kPa regardless of this flag.}

\item{verbose}{If \code{TRUE} (the default), show progress and diagnostic
messages.}
}
\value{
A \pkg{units} vector of TWL values in W/m^2 (a genuine physical
quantity). Use \code{\link[=categorise_twl]{categorise_twl()}} / \code{\link[=twl_colour]{twl_colour()}} (both units-aware) to
map it to zones, or \code{units::drop_units()} for a bare numeric.
}
\description{
Calculates the maximum sustainable metabolic rate (W/m^2) that
well-hydrated, acclimatised individuals can maintain in a specific
thermal environment. Based on the Brake & Bates (2002) methodology.
}
\details{
Weather parameters (\code{temp}, \code{wind_speed}, \code{RH}, \code{direct_solar},
\code{diffuse_solar}, \code{pressure}) that are not supplied are automatically
retrieved from the \href{https://open-meteo.com/}{Open-Meteo API} using the
provided \code{datetime}, \code{latitude} and \code{longitude}. An internet connection
is required for auto-fetching.

Open-Meteo returns wind speed at 10 m height (\code{wind_speed_10m}). This is
corrected to approximately 1 m (body level) using a logarithmic wind
profile with roughness length z0 = 0.01 m (open terrain), giving a
correction factor of \code{ln(1/z0) / ln(10/z0)} ≈ 0.667. Supply \code{wind_speed}
directly (already at body level) to skip this correction.

TWL categories (from Brake & Bates 2002):
\itemize{
\item > 220 W/m^2: Unrestricted work
\item 140--220 W/m^2: Acclimatisation zone
\item 115--140 W/m^2: Buffer zone
\item < 115 W/m^2: Withdrawal required
}
}
\section{Units}{

The dimensional weather inputs (\code{temp} degC, \code{wind_speed} m/s, \code{direct_solar}
/ \code{diffuse_solar} W/m^2, \code{pressure}, \code{wet_bulb} degC) and \code{max_core_temp}
(degC) may each be supplied as a bare numeric in the documented unit or as a
\pkg{units} object, which is converted automatically (a dimensionally
incompatible unit is an error). \code{RH} is a percentage and is taken as-is. A
units-tagged \code{pressure} is converted to kPa directly, bypassing the
\code{convert_pressure} hPa/kPa flag (which only governs bare pressure input).
}

\references{
Brake, D.J. and Bates, G.P. (2002) Limiting Metabolic Rate (Thermal Work
Limit) as an Index of Thermal Stress. Applied Occupational and Environmental
Hygiene, 17:3, 176-186.
}
