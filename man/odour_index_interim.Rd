% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odour_exposure.R
\name{odour_index_interim}
\alias{odour_index_interim}
\title{Interim 0-100 odour index (uncalibrated screening map)}
\usage{
odour_index_interim(rel, map_c50 = 0.3)
}
\arguments{
\item{rel}{A relative-concentration matrix (\code{n_hours x n_receptors}) from
\code{\link[=odour_exposure]{odour_exposure()}}, or a plain numeric vector.}

\item{map_c50}{Relative concentration at which the map reaches ~63
(\code{100 * (1 - exp(-C_rel / map_c50))}). Default 0.3 (uncalibrated).}
}
\value{
A plain numeric vector of length \code{nrow(rel)} (or \code{length(rel)} for a
vector input): the worst-case 0-100 odour index across receptors per hour.
}
\description{
Collapses the per-receptor relative concentration matrix returned by
\code{\link[=odour_exposure]{odour_exposure()}} / \code{\link[=odour_risk]{odour_risk()}} into a single worst-case 0-100 band per
hour, using the saturating map \code{100 * (1 - exp(-C_rel / map_c50))}.
}
\section{Status — interim, awaiting the calibration helper}{

This is the previous default behaviour, retained but \strong{parked}. The
\code{map_c50} reference and the downstream tier cut-points in \code{\link[=categorise_odour]{categorise_odour()}}
are uncalibrated screening defaults (issue #8). The package now returns the
physical relative concentration from \code{\link[=odour_exposure]{odour_exposure()}}; turning that into a
site-meaningful index is a calibration step that belongs to the consuming
dashboard. A forthcoming calibration helper (issues #11/#8) will fit this
mapping from complaint / observation records. Until then this function gives
a defensible, clearly-flagged screening number — do not treat it as
calibrated or as comparable across sites.
}

\seealso{
\code{\link[=odour_exposure]{odour_exposure()}}, \code{\link[=categorise_odour]{categorise_odour()}}
}
