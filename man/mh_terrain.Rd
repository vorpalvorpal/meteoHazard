% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site-model.R
\name{mh_terrain}
\alias{mh_terrain}
\title{Pinned topographic terrain descriptors}
\usage{
mh_terrain(
  relief = NA_real_,
  valley_depth = NA_real_,
  basin_capacity = NA_real_,
  drainage_bearing = NA_real_,
  flow_convergence = NA_real_,
  slope = NA_real_,
  aspect = NA_real_,
  taf = NA_real_,
  shelter_index = NA_real_,
  meta = list()
)
}
\arguments{
\item{relief}{Non-negative numeric or units object. Height above local base
in metres (NOT a standardised deviation index).}

\item{valley_depth}{Non-negative numeric or units object. Depth of the
containing valley in metres.}

\item{basin_capacity}{Non-negative numeric or units object. Capacity of the
containing basin in m^3.}

\item{drainage_bearing}{Numeric. Dominant drainage-flow bearing in degrees,
in \verb{[0, 360)}.}

\item{flow_convergence}{Numeric. Dimensionless flow-convergence index; must
be finite (not Inf/NaN).}

\item{slope}{Numeric. Mean slope angle in degrees.}

\item{aspect}{Numeric. Aspect angle in degrees.}

\item{taf}{Numeric. Topographic amplification factor (dimensionless, >= 1).}

\item{shelter_index}{Numeric. Openness-based shelter index in degrees; must
be finite (not Inf/NaN).}

\item{meta}{Named list. Scale fields: \code{relief_radius}, \code{channel_threshold},
\code{fetch_L}, \code{dem_resolution}, \code{datum}.}
}
\value{
An \code{mh_terrain} S7 object.
}
\description{
Constructs a validated \code{mh_terrain} object holding the scalar terrain
descriptors used by the meteoHazard dispersion and hazard models. All
descriptors are optional; pass \code{NA} (the default) for a flat site with no
terrain correction.
}
\details{
Dimensional arguments (\code{relief}, \code{valley_depth}, \code{basin_capacity}) accept a
bare numeric (assumed already in the documented unit) or a \code{\link[units:units]{units}}
object (converted; dimensional mismatch errors). They are stored internally
as plain doubles in canonical units.
}
