% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/litter_wetness.R
\name{litter_wetness_vec}
\alias{litter_wetness_vec}
\title{Hourly litter-surface wetness state}
\usage{
litter_wetness_vec(
  precipitation,
  temperature_2m,
  relative_humidity_2m,
  wind_speed_10m,
  shortwave_radiation,
  wetness_set_precip = 0.5,
  dry_rate_base = 0.7,
  vpd_coef = 1,
  wind_coef = 0.1,
  sw_coef = 0.5,
  sw_ref = 500,
  w0 = 0
)
}
\arguments{
\item{precipitation}{Numeric vector. Hourly precipitation (mm),
Open-Meteo \code{precipitation}. Drives the rain-reset trigger.}

\item{temperature_2m}{Numeric vector. Air temperature at 2 m (degC),
Open-Meteo \code{temperature_2m}. Feeds the Tetens VPD calculation. Not
bounds-checked -- sub-zero values are physically valid.}

\item{relative_humidity_2m}{Numeric vector. Relative humidity at 2 m (\%),
Open-Meteo \code{relative_humidity_2m}, in \code{0}-\code{100}. Feeds the Tetens VPD
calculation.}

\item{wind_speed_10m}{Numeric vector. Mean wind speed at 10 m (m/s),
Open-Meteo \code{wind_speed_10m}. Increases the drying rate (evaporative
ventilation of the thin film).}

\item{shortwave_radiation}{Numeric vector. Incoming shortwave radiation
(W/m^2), Open-Meteo \code{shortwave_radiation}. Increases the drying rate
(radiative/convective heating of the surface).}

\item{wetness_set_precip}{Hourly precipitation (mm) at or above which the
surface is reset to fully wet (\code{w = 1}), overriding the prior hour's
state. Default \code{0.5}.}

\item{dry_rate_base}{Base exponential drying rate (per hour) at zero VPD,
calm wind, and no insolation. Default \code{0.7}. High calibration
uncertainty.}

\item{vpd_coef}{Drying-rate gain per kPa of vapour-pressure deficit.
Default \code{1.0}. High calibration uncertainty.}

\item{wind_coef}{Drying-rate gain per m/s of mean wind. Default \code{0.1}.
High calibration uncertainty.}

\item{sw_coef}{Drying-rate gain per unit of \code{shortwave_radiation / sw_ref}.
Default \code{0.5}. High calibration uncertainty.}

\item{sw_ref}{Reference insolation (W/m^2) that normalises
\code{shortwave_radiation} in the drying-rate term. Default \code{500}.}

\item{w0}{Initial wetness state, in \verb{[0, 1]}, assumed for the hour just
before row 1 (i.e. what the surface looked like going into the forecast
window). Default \code{0} (dry start).}
}
\value{
Numeric vector of length equal to the inputs, the litter-surface
wetness state (dimensionless, \verb{[0, 1]}) for each forecast hour.
}
\description{
Computes a sequential (hour-over-hour) surface-wetness state \code{w} in
\verb{[0, 1]} for windblown litter: an hour with enough rain resets the surface
to fully wet (\code{w = 1}); otherwise the surface dries exponentially at a rate
driven by vapour-pressure deficit (VPD), wind, and insolation. This is a
fast, thin-film state deliberately distinct from the gridded
\code{soil_moisture_0_to_1cm} layer used elsewhere in the package: soil-moisture
memory operates on a scale of \emph{days} (McColl et al. 2017,
\doi{10.1038/ngeo2868}), whereas a film of water or a wet paper/plastic
surface holds well under a millimetre and dries off in a matter of hours
under sun/wind (Zhou et al. 2021, \doi{10.3390/ijerph18041790}). Feeding
\code{\link[=litter_hazard_vec]{litter_hazard_vec()}} the slow soil-moisture proxy when a fast-drying
litter surface is actually what governs entrainment understates the
litter hazard in the hours just after a shower; this state is the
fast-memory alternative, supplied to \code{\link[=litter_hazard_vec]{litter_hazard_vec()}} via its
\code{wetness} argument.
}
\details{
The drying-rate form (VPD x wind x insolation multipliers on a base rate)
mirrors the aerodynamic-plus-radiative structure of open-surface evaporation
(Penman 1948, \doi{10.1098/rspa.1948.0037}: evaporation rises with the
vapour-pressure deficit, wind, and available radiation) and the
leaf-wetness-duration literature, where those same drivers govern the drying
phase of a surface-wetness event (Sentelhas et al. 2008,
\doi{10.1016/j.agrformet.2007.09.011}). All rate coefficients here
(\code{dry_rate_base}, \code{vpd_coef}, \code{wind_coef}, \code{sw_coef}) are litter-specific
calibration placeholders, not values taken from a validated litter/leaf
study -- flagged high calibration uncertainty throughout.
}
\section{Units}{

\code{precipitation}, \code{wind_speed_10m}, and \code{shortwave_radiation} may be
supplied either as bare numerics in the documented unit or as
\pkg{units} objects, which are converted automatically (a dimensionally
incompatible unit is an error). \code{temperature_2m} and
\code{relative_humidity_2m} are taken as plain numerics (degC, \%). The
returned wetness state is dimensionless and is a plain numeric.
}

\references{
McColl, K. A. et al. (2017). Global characterization of surface
soil-moisture drydowns. \emph{Nature Geoscience} 10, 100-104.
\doi{10.1038/ngeo2868}. Motivates a fast litter-wetness state distinct
from the (multi-day-memory) gridded soil-moisture layer.

Zhou, Q. et al. (2021). Thin liquid films on solid surfaces: formation,
properties and health implications. \emph{International Journal of
Environmental Research and Public Health} 18(4), 1790.
\doi{10.3390/ijerph18041790}. Basis for treating litter surface wetness as
a thin film (sub-mm capacity) that dries off quickly.

Tetens, O. (1930). Uber einige meteorologische Begriffe.
\emph{Zeitschrift fur Geophysik} 6, 297-309. Basis for the saturation
vapour pressure approximation used in \code{.litter_vpd()}.
}
\seealso{
\code{\link[=litter_hazard_vec]{litter_hazard_vec()}} for how \code{wetness} is consumed by the
hazard index (\code{use_wetness_state = TRUE} in \code{\link[=litter_hazard]{litter_hazard()}}).
}
