% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dust_hazard.R
\name{dust_hazard}
\alias{dust_hazard}
\title{Dust hazard: crust-adjusted relative dust flux}
\usage{
dust_hazard(
  met_data,
  tyler_sieve_no = 20L,
  clay_percent = 10,
  d50 = NULL,
  z0 = NULL,
  bulk_density = 1.6,
  gust_factor = 0.84,
  forcing = c("gust", "weibull"),
  weibull_shape = DUST_CONSTANTS$WEIBULL_SHAPE,
  air_density = c("reference", "met"),
  crust = FALSE,
  rain_crust_threshold = 2,
  crust_factor_max = 3,
  crust_decay_hours = 72,
  hours_since_last_rain = Inf
)
}
\arguments{
\item{met_data}{A tibble (or data frame), one row per hourly timestep, with at
least \code{wind_speed_10m} (m/s), \code{wind_gusts_10m} (m/s), and
\code{soil_moisture_0_to_1cm} (m^3/m^3); plus \code{precipitation} (mm) when
\code{crust = TRUE}; plus \code{temperature_2m} (deg C) and \code{surface_pressure} (hPa)
when \code{air_density = "met"}.}

\item{tyler_sieve_no, clay_percent, d50, z0, bulk_density, gust_factor, forcing, weibull_shape}{Site and model parameters forwarded to \code{\link[=dust_flux]{dust_flux()}}. \code{d50} (m), if
supplied, supersedes \code{tyler_sieve_no} (with a warning if both are
non-\code{NULL}); \code{forcing = "weibull"} ignores \code{wind_gusts_10m}/\code{gust_factor}
-- see \code{\link[=dust_flux]{dust_flux()}}.}

\item{air_density}{\code{"reference"} (default) or \code{"met"}. \code{"reference"} uses
the fixed \code{DUST_CONSTANTS$RHO_A_REF} and ignores any \code{temperature_2m}/
\code{surface_pressure} columns in \code{met_data}, even if present (an explicit
flag, not auto-detection, so a caller never gets a silent behaviour
change from adding those columns). \code{"met"} requires both columns and
forwards them to \code{\link[=dust_flux]{dust_flux()}} for a per-hour, temperature/pressure-driven
air density.}

\item{crust}{Logical. Enable the precipitation-driven crust-persistence gate.
Default \code{FALSE}. When \code{TRUE}, \code{met_data$precipitation} is required.}

\item{rain_crust_threshold}{Precipitation (mm) at or above which an hour is
treated as a crust-forming rain event. Default 2.}

\item{crust_factor_max}{Maximum threshold multiplier immediately after rain.
Default 3.}

\item{crust_decay_hours}{E-folding time (hours) over which the crust
suppression decays. Default 72.}

\item{hours_since_last_rain}{Assumed crust age (hours) going into row 1 of
\code{met_data}, i.e. how long since the last crust-forming rain before the
forecast window starts. Default \code{Inf} (no crust memory at row 1,
reproducing the pre-v3 behaviour). Set to \code{0} if the forecast is known to
start immediately after a crust-forming rain event; see @section Crust
cold-start.}
}
\value{
Numeric vector of length \code{nrow(met_data)}, the crust-adjusted
\strong{relative dust flux} for each forecast hour (same units as \code{\link[=dust_flux]{dust_flux()}};
unbounded, \code{0} below the entrainment threshold). Issue #11 removed the fixed
0-100 dust index in favour of this physical output.
}
\description{
Computes an hourly \strong{relative dust flux} for each row of a meteorological
forecast tibble: a \code{met_data}-frame convenience wrapper over \code{\link[=dust_flux]{dust_flux()}}
that adds the precipitation-driven crust-persistence gate. The returned flux
is in the same relative units as \code{\link[=dust_flux]{dust_flux()}} (a strong gust on a dry
surface produces a large value; a sub-threshold or wet/crusted hour produces
near-zero).
}
\section{Status — physical output, no fixed operational scale (issue #11)}{

This used to return a 0-100 index normalised against a reference gust. That
fixed 0-100 scale (and the \code{categorise_dust()} tiers) was removed: the
package now emits the physical relative flux, and mapping it onto a
site-specific operational index is a calibration step delivered by
forthcoming calibration tooling (issues #11/#8), not by fixed cut-points.
}

\section{Crust cold-start}{

The precipitation-driven crust gate has no look-back before row 1 of
\code{met_data}: unless told otherwise, it assumes row 1 has no crust memory
(equivalent to "it has not rained in a very long time"). To represent a
forecast that starts partway through a crust-decay window, either (a)
prepend roughly \code{3 * crust_decay_hours} of history to \code{met_data} so the
crust state has settled by the row of interest, or (b) set
\code{hours_since_last_rain} to the actual elapsed time since the last
crust-forming rain before the forecast window starts.
}

\section{Idealisations}{

The precipitation-driven crust factor is an uncalibrated exponential
clock-decay placeholder (\code{1 + (crust_factor_max - 1) * exp(-age / crust_decay_hours)}): real crust breakdown is primarily mechanical
(abrasion/rupture by saltating-particle impact), not a pure function of
elapsed time (Gillette et al. 1982; Rice & McEwan 2001). A future version
should gate crust decay on cumulative saltation activity (a TODO, not
implemented here) rather than the clock alone. Separately, watering and
other surface management (the first-order dust-suppression control on an
active landfill working face) are invisible to the gridded
\code{soil_moisture_0_to_1cm} input; \code{threshold_multiplier} (via \code{\link[=dust_flux]{dust_flux()}})
is the intended injection hook — e.g. a caller can locally raise the
threshold multiplier during known watering hours before invoking
\code{\link[=dust_flux]{dust_flux()}} directly.
}

\seealso{
\code{\link[=dust_flux]{dust_flux()}}.
}
