% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odour_exposure.R
\name{odour_exposure}
\alias{odour_exposure}
\title{Landfill odour exposure (direction- and geometry-aware, mh_site API)}
\usage{
odour_exposure(
  met_data,
  site,
  stability = c("turner", "shear"),
  terrain_backend = c("none", "descriptors"),
  shelter = FALSE,
  shelter_h_mix = FALSE,
  rim_venting = FALSE
)
}
\arguments{
\item{met_data}{A data frame, one row per consecutive hourly timestep. Must
contain at minimum: \code{wind_direction_10m}, \code{wind_speed_10m},
\code{direct_radiation}, \code{cloud_cover}, \code{boundary_layer_height}. Columns for
the generation modifier \code{G} (\code{pressure_msl}, \code{precipitation},
\code{temperature_2m}, \code{relative_humidity_2m}, \code{soil_moisture_0_to_1cm},
\code{soil_moisture_1_to_3cm}) are optional; if absent \code{G} defaults to 1.}

\item{site}{An \code{\link[=mh_site]{mh_site()}} object with at least one \verb{(odour, source)} role
and one \verb{(odour, receptor)} role.}

\item{stability}{Stability estimator: \code{"turner"} (default) or \code{"shear"}.}

\item{terrain_backend}{\code{"none"} (flat Gaussian, C3a) or \code{"descriptors"}
(terrain-aware morning pulse, wired in C3b; C3a returns the flat result).}

\item{shelter}{Logical. Passed to \code{\link[=ventilation_state]{ventilation_state()}}; when \code{TRUE}, M3
valley sheltering reduces \code{u_eff}, which enters the exposure numerator via
the shared \code{.odour_hazard_raw()} core — the effect propagates end-to-end
through \code{odour_risk()} after the C9 fix.}

\item{shelter_h_mix}{Logical. Passed to \code{\link[=ventilation_state]{ventilation_state()}}.}

\item{rim_venting}{Logical. When \code{TRUE} and \code{terrain_backend = "descriptors"},
activates C8 upslope rim-venting: the morning pathway 1a crosswind factor is
gated by a vertical reach function and scaled by per-receptor upslope
alignment. Has no effect when all receptor \code{rel_elevation} values are zero or
absent. Default \code{FALSE} (C8 not yet calibrated; see issue #8).}
}
\value{
A plain numeric matrix with \code{nrow(met_data)} rows (one per hour) and
one column per \verb{(odour, receptor)} feature (column names are the receptor
\code{id}s): the \strong{relative} odour concentration at each receptor for each
hour. Unbounded, dimensionless, and referenced to a Briggs class-F plume at
\code{ODOUR_CONSTANTS$X_REF_EXPOSURE}. Coincident source/receptor pairs and
upwind receptors read 0. Reduce over columns for a worst-case summary
(e.g. \code{apply(out, 1, max)}); any mapping onto an operational index is a
site-specific calibration step (see issues #11/#8).
}
\description{
Maps the atmospheric ventilation state onto a per-receptor \strong{relative
concentration} given the wind direction, the source geometry, and the
receptor layout described by an \code{\link[=mh_site]{mh_site()}} object. For each hour the
relative concentrations from all \verb{(odour, source)} features are summed at
each \verb{(odour, receptor)}. The result is the unbounded physical layer: a
\code{n_hours x n_receptors} matrix.

Turning this physical value into a bounded operational index (e.g. 0-100) or
into site-specific tiers is a \strong{calibration} decision that belongs downstream
of the package. The package no longer ships a fixed 0-100 odour scale;
site-specific calibration tooling is planned (issues #11/#8).
}
\details{
Area sources use ISC3 initial spreads: \code{sigma_y0 = crosswind_halfwidth / 4.3} and \code{sigma_z0 = emit_height / 2.15} (both added in quadrature with the
Briggs physical spread). Point sources have \code{sigma_y0 = sigma_z0 = 0}.
}
\section{Units}{

Dimensional \code{met_data} columns (see \code{\link[=odour_hazard]{odour_hazard()}}) may be supplied as
bare numerics (documented unit) or \pkg{units} objects (converted
automatically; mismatch errors). Bearings and angles are in degrees and
taken as-is.
}

\references{
Briggs, G.A. (1973). \emph{Diffusion Estimation for Small Emissions}. NOAA.
US EPA (1995). \emph{User's Guide for the Industrial Source Complex (ISC3)
Dispersion Models}.
}
\seealso{
\code{\link[=odour_hazard]{odour_hazard()}}, \code{\link[=odour_risk]{odour_risk()}}, \code{\link[=ventilation_state]{ventilation_state()}}, \code{\link[=mh_site]{mh_site()}}
}
