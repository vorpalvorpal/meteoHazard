% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site-constructors.R
\name{site_from_sectors}
\alias{site_from_sectors}
\title{Build an mh_site from compass-sector barrier descriptions}
\usage{
site_from_sectors(sectors, centroid, ring_radius = 1000, epsg)
}
\arguments{
\item{sectors}{data.frame with columns:
\describe{
\item{\code{arc_start}, \code{arc_end}}{Compass labels (\code{N}, \code{NE}, \code{E}, \code{SE},
\code{S}, \code{SW}, \code{W}, \code{NW}) giving the clockwise start and end of each
boundary sector. \code{arc_start == arc_end} names a full-circle sector
(a ring enclosing the source), not a zero-area sliver.}
\item{\code{permeability}}{Numeric \verb{[0, 1]}.}
\item{\code{sensitive}}{Logical.}
\item{\code{distance_m}}{Optional numeric \verb{> 0} (metres): the reach
distance used by \code{\link[=litter_exposure]{litter_exposure()}}'s refined distance-reach mode.
If absent, every row defaults to \code{ring_radius}.}
}}

\item{centroid}{An \code{sf} object containing a single POINT feature in any
CRS.  Must not be \code{NULL}.  If the CRS is geographic it will be
reprojected to \code{epsg} before use.}

\item{ring_radius}{Numeric (metres). Radius of the ring on which barrier
arc polygons are placed. Default \code{1000}.}

\item{epsg}{Integer. Projected metric EPSG code passed to \code{\link[=mh_site]{mh_site()}}.}
}
\value{
An \code{\link{mh_site}} object.
}
\description{
Converts a compass-sector data frame (the format accepted by the legacy
\code{litter_exposure()} \code{site} argument) into an \code{\link{mh_site}} S7 object.
A wedge-shaped polygon is constructed for each sector row on a ring of
radius \code{ring_radius} centred on \code{centroid}. The returned site has:
}
\details{
\itemize{
\item A \verb{(litter, source)} point feature at \code{centroid} (id \code{"source"}).
\item One \verb{(litter, barrier)} polygon per sector row (ids \code{"barrier_1"},
\code{"barrier_2"}, ...), carrying numeric columns \code{bearing_start} and
\code{bearing_end} so that downstream functions can use bearing containment
tests directly.
}
}
