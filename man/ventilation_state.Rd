% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odour-ventilation.R
\name{ventilation_state}
\alias{ventilation_state}
\title{Atmospheric ventilation state for the odour dispersion model}
\usage{
ventilation_state(
  met_data,
  terrain = NULL,
  stability = c("turner", "shear"),
  shelter = FALSE,
  shelter_h_mix = FALSE,
  pool_cap = TRUE,
  odorant_solubility = ODOUR_CONSTANTS$ODORANT_SOLUBILITY_DEFAULT
)
}
\arguments{
\item{met_data}{A data frame, one row per consecutive hour. Required columns:
\code{wind_speed_10m} (m/s), \code{direct_radiation} (W/m^2), \code{cloud_cover} (\\%),
\code{boundary_layer_height} (m). Optional columns: \code{temperature_2m} (°C) and
\code{relative_humidity_2m} (\\%) for the Brunt cooling rate; multi-level winds
\code{wind_speed_80m}, \code{wind_direction_80m}, \code{wind_speed_120m},
\code{wind_direction_120m}, \code{wind_speed_180m}, \code{wind_direction_180m} for
\code{residual_wind}. Missing optional columns yield \code{NA} in the corresponding
output fields (not an error). Malformed optional columns (present but
non-numeric) are an error.}

\item{terrain}{An \code{\link[=mh_terrain]{mh_terrain()}} object, or \code{NULL} (default, flat site).
When non-\code{NULL}, \code{terrain@valley_depth} caps the cold-pool depth and
\code{terrain@taf} amplifies the Brunt cooling rate before accumulation.}

\item{stability}{Stability estimator: \code{"turner"} (default) or \code{"shear"}.
\code{"shear"} requires \code{wind_speed_80m}.}

\item{shelter}{Logical (default \code{FALSE}). When \code{TRUE} and \code{terrain} has a
finite \code{shelter_index}, reduces \code{u_eff} using the M3 valley-sheltering
transfer model. \strong{Off by default until calibrated} (see #8). \code{FALSE}
gives bit-identical output to the pre-C6 baseline.}

\item{shelter_h_mix}{Logical (default \code{FALSE}). When \code{TRUE} (and
\code{shelter = TRUE}), also scales \code{h_mix} by \code{(1 - reduction_effective)}.
Undocumented interaction with the forecast \code{boundary_layer_height}
(potential double-count); leave \code{FALSE} until reconciled.}

\item{pool_cap}{Logical (default \code{TRUE}). When \code{TRUE}, caps \code{h_mix} at the
nocturnal cold-pool depth (\code{pool_top}) on stable nights -- see the
\code{h_mix} return-value bullet below. \code{FALSE} reproduces the pre-v3
raw-boundary-layer-height behaviour.}

\item{odorant_solubility}{Number in \verb{[0, 1]} (default
\code{ODOUR_CONSTANTS$ODORANT_SOLUBILITY_DEFAULT}, 0.5). Blends \code{W_rain}
between no washout (0, poorly-soluble reduced-sulfur odorants) and the
soluble-limit tiers (1, highly-soluble ammonia/amines/VOCs).}
}
\value{
A named list, all vectors of length \code{nrow(met_data)}:
\describe{
\item{\code{u_eff}}{Effective wind speed (m/s), floored at \code{U_CALM_FLOOR}.}
\item{\code{h_mix}}{Mixing depth (m), positive; falls back to stable/unstable
constant when \code{boundary_layer_height} is \code{NA} or non-positive. By
default the nocturnal cold-pool depth (\code{pool_top}) caps it on stable
nights (\code{pool_cap = TRUE}); set \code{pool_cap = FALSE} for the raw
boundary-layer-height behaviour.}
\item{\code{s}}{Pasquill-Gifford stability index in [0, 5] (A = 0, F = 5).}
\item{\code{is_calm}}{Logical; wind below \code{U_CALM_FLOOR} or \code{NA}.}
\item{\code{is_day}}{Logical; direct radiation > 10 W/m^2.}
\item{\code{PM}}{Peak-to-mean ratio, interpolated between \code{PM_MIN} (s = 0)
and \code{PM_MAX} (s = 5).}
\item{\code{W_rain}}{Below-cloud scavenging factor in (0, 1]; requires
\code{precipitation} in \code{met_data} (falls back to 1.0 when absent/NA).}
\item{\code{pool_top}}{Nocturnal cold-pool depth (m). Accumulated nightly from
Brunt net-longwave cooling then Whiteman (1999) saturation depth, floored
by the Venkatram (1980) mechanical depth. Frozen at morning release and
reset at next nightfall. \code{NA} when temperature and RH are unavailable.}
\item{\code{cbl_growth}}{Convective boundary-layer growth rate (m/h); positive
during morning transition, zero otherwise.}
\item{\code{residual_wind}}{Named list with elements \code{speed_80m}, \code{dir_80m},
\code{speed_120m}, \code{dir_120m}, \code{speed_180m}, \code{dir_180m}, \code{speed_10m},
\code{dir_10m}: per-hour overnight circular-mean direction (degrees) and
concurrent speed at each level. The 10 m surface-wind level is a last-
resort fallback used when all upper-level columns are absent; it
represents the surface-layer wind inside the boundary layer rather than
the decoupled residual layer above. \code{NA} for absent or all-daytime
windows.}
}
}
\description{
Computes the per-hour atmospheric state used by \code{\link[=odour_hazard]{odour_hazard()}} and
\code{\link[=odour_exposure]{odour_exposure()}}, extended with the nocturnal cold-pool depth
(\code{pool_top}), convective boundary-layer growth rate (\code{cbl_growth}), and
overnight residual winds at 80 m / 120 m / 180 m AGL.
}
\section{Terrain-modifier contract}{

When multiple terrain mechanisms are active simultaneously, the following
precedence applies: \strong{M1 drainage confinement} (C3b) supersedes \strong{M3
valley sheltering} on any hour when \code{drainage_active} is \code{TRUE}
(\code{is_channelled & !is_day & pool_top > 0}). The suppression strength is
controlled by \code{ODOUR_CONSTANTS$DRAINAGE_SHELTER_OVERLAP} (default 1.0 =
full mutual exclusion). A future external wind field (C7) will supersede all
native terms.

The M3 hazard/exposure re-entanglement caveat: enabling \code{shelter = TRUE}
makes \code{ventilation_state()} depend on a terrain descriptor, so the
ventilation state is no longer purely meteorological. This is inherent to
M3; keep \code{shelter = FALSE} in all meteorological-analysis contexts.

With \code{terrain = NULL} the nocturnal cold-pool cap (\code{pool_cap = TRUE}) is
still purely meteorological -- \code{pool_top} is derived from Brunt/Whiteman/
Venkatram formulas over \code{met_data} alone. It becomes terrain-modulated only
when \code{terrain} supplies \code{taf} and/or \code{valley_depth} (see \code{\link[=mh_terrain]{mh_terrain()}}).
}

\references{
Brunt, D. (1932). Notes on radiation in the atmosphere. \emph{Quarterly
Journal of the Royal Meteorological Society}, 58(247), 389--420.
Whiteman, C.D. (1999). Wintertime evolution of the temperature inversion
in the Sinbad Valley. \emph{Journal of Applied Meteorology}, 38, 178--188.
Venkatram, A. (1980). Estimating the Monin-Obukhov length in the stable
boundary layer for dispersion calculations. \emph{Boundary-Layer
Meteorology}, 19, 481--485.
}
\seealso{
\code{\link[=odour_hazard]{odour_hazard()}}, \code{\link[=odour_exposure]{odour_exposure()}}
}
