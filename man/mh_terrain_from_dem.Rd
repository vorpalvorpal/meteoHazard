% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/terrain-from-dem.R
\name{mh_terrain_from_dem}
\alias{mh_terrain_from_dem}
\title{Derive terrain descriptors from a DEM}
\usage{
mh_terrain_from_dem(
  dem,
  source,
  receptors = NULL,
  epsg,
  conditioning = c("breach", "fill", "none"),
  shelter_fetch_m = NULL,
  ...
)
}
\arguments{
\item{dem}{A \code{terra::SpatRaster}, an absolute file path to a raster file, or
an \code{sf} bounding box (triggers an \code{elevatr} download at setup time).}

\item{source}{An \code{sf} object containing the source location(s). Used to
determine the descriptor reference point.}

\item{receptors}{An \code{sf} object of receptor points, or \code{NULL} (default).
When supplied, per-receptor \code{rel_elevation} and \code{hill_height_scale} are
derived and returned as \verb{$receptor_fields}.}

\item{epsg}{Integer. EPSG code for the projected metric CRS used for all
geometry. Must be a projected (non-geographic) CRS.}

\item{conditioning}{Breach/fill conditioning applied to the DEM before flow
routing: \code{"breach"} (default, least-cost breach), \code{"fill"}, or \code{"none"}.}

\item{shelter_fetch_m}{Numeric. Lookup radius in metres for the topographic
openness (shelter_index) calculation. \code{NULL} (default) derives a
physically-motivated default from the DEM scan range.}

\item{...}{Additional arguments reserved for future use (caller descriptor
overrides are passed here; see Details).}
}
\value{
When \code{receptors = NULL}: a validated \code{mh_terrain} S7 object with
every applicable scalar descriptor and \verb{@meta} recording the analysis
scale for each derived descriptor plus \code{dem_resolution} and \code{dem_source}.
When \code{receptors} is an \code{sf} object: a named list with
\verb{$terrain} (the \code{mh_terrain}) and \verb{$receptor_fields} (a data frame with
columns \code{feature_id}, \code{rel_elevation}, \code{hill_height_scale}).
}
\description{
A setup-time helper that derives the C1 \code{mh_terrain} descriptors from a
digital elevation model using data-driven analysis scales. The result is a
frozen descriptor table for the run path. \strong{Not for use on the run path} —
deriving descriptors is expensive (minutes); the run path reads the saved
\code{mh_terrain} object.
}
\details{
\strong{Caller overrides:} any descriptor name (\code{relief}, \code{valley_depth},
\code{basin_capacity}, \code{taf}, \code{shelter_index}, \code{drainage_bearing},
\code{flow_convergence}, \code{slope}, \code{aspect}) may be passed as a named scalar in
\code{...} to skip its DEM derivation and use the supplied value directly. The
corresponding \code{meta} entry is set to \code{"user_supplied"}.
}
\section{Setup-time only}{

This function runs WhiteboxTools flow-routing and openness calculations that
take seconds to minutes. Call it once at site setup, save the returned
\code{mh_terrain}, and pass that to \code{\link[=odour_exposure]{odour_exposure()}}, \code{\link[=odour_risk]{odour_risk()}}, etc.
}

\section{Dependencies}{

Requires the \code{whitebox} R package and the WhiteboxTools binary
(\code{whitebox::install_whitebox()}), and the \code{terra} package.
}

\references{
Lindsay, J.B. (2015). Geomorphons: a new automated landform classification
method for characterizing the topographic position of land. \emph{Geomorphology},
238, 18--29.
Yokoyama, R., Shirasawa, M., & Pike, R.J. (2002). Visualizing topography by
openness: a new application of image processing to digital elevation models.
\emph{Photogrammetric Engineering and Remote Sensing}, 68(3), 257--265.
}
