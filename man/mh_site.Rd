% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site-model.R
\name{mh_site}
\alias{mh_site}
\title{Geo-referenced site model}
\usage{
mh_site(features, roles, terrain = NULL, epsg, datum = "AGL")
}
\arguments{
\item{features}{An \code{sf} object with at least an \code{id} column and a geometry
column. May optionally carry \code{elevation} and \code{emit_height} columns.}

\item{roles}{A data.frame with columns \code{feature_id}, \code{hazard}, and \code{role}.
Optional columns: \code{weight}, \code{sensitive}, \code{permeability}. Every
\code{feature_id} must exist in \code{features$id}.}

\item{terrain}{An \code{mh_terrain} object or \code{NULL} (default).}

\item{epsg}{Integer. EPSG code for the projected metric CRS to use
internally. Must identify a projected (non-geographic) CRS. Features in any
other CRS (geographic or a different projection) are reprojected to \code{epsg}
automatically.}

\item{datum}{Character. Vertical datum string. Currently only \code{"AGL"} is
recognised.}
}
\value{
An \code{mh_site} S7 object.
}
\description{
Bundles an \code{sf} feature collection, a roles table, optional terrain
descriptors, a projected CRS, and a vertical datum into a single validated
domain object.
}
