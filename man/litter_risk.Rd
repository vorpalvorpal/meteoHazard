% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/litter_risk.R
\name{litter_risk}
\alias{litter_risk}
\title{Landfill litter risk (combined hazard + exposure)}
\usage{
litter_risk(
  met_data,
  site,
  use_wetness_state = FALSE,
  direction_tol = 15,
  p_open_min = 0.5,
  move_threshold = 20,
  offsite_threshold = 45,
  default_permeability = 0.5,
  reach_per_ms = NULL,
  ...
)
}
\arguments{
\item{met_data}{A tibble (or data frame), one row per hourly timestep. Must
contain \code{wind_direction_10m} (degrees) plus the columns \code{\link[=litter_hazard]{litter_hazard()}}
requires for the chosen \code{use_wetness_state}. When \code{reach_per_ms} is
supplied it must also contain \code{wind_speed_10m} (m/s), forwarded as the
refined-reach mean wind.}

\item{site}{An \code{\link{mh_site}} S7 object; see \code{\link[=litter_exposure]{litter_exposure()}}.}

\item{use_wetness_state}{Logical (default \code{FALSE}). Passed to
\code{\link[=litter_hazard]{litter_hazard()}}: when \code{TRUE}, the entrainment wetness signal is the
drying litter-wetness state (\code{\link[=litter_wetness]{litter_wetness()}}) instead of soil moisture.}

\item{direction_tol, p_open_min, move_threshold, offsite_threshold, default_permeability}{Exposure-layer parameters, passed to \code{\link[=litter_exposure]{litter_exposure()}}.}

\item{reach_per_ms}{Optional positive scalar (metres per m/s). When supplied,
\code{\link[=litter_exposure]{litter_exposure()}} runs in refined distance-reach mode with
\code{mean_wind = met_data$wind_speed_10m}. Because the mean wind then drives
both the hazard's transport multiplier and the exposure reach test, the
transport ramp and \code{reach_per_ms} should be calibrated jointly — see
\code{\link[=litter_hazard_vec]{litter_hazard_vec()}} and \code{\link[=litter_exposure]{litter_exposure()}}.}

\item{...}{Additional hazard calibration parameters forwarded to
\code{\link[=litter_hazard]{litter_hazard()}} (e.g. \code{rain_threshold}, \code{material}, \code{gust_threshold}).}
}
\value{
The \code{\link[=litter_exposure]{litter_exposure()}} data frame (\code{exposure}, \code{zone},
\code{directional_factor}, \code{leaves_site}, \code{sensitive_receptor}), one row per
forecast hour.
}
\description{
Convenience wrapper that computes the windblown-litter hazard
(\code{\link[=litter_hazard]{litter_hazard()}}) and maps it through the geometry-aware
\code{\link[=litter_exposure]{litter_exposure()}} layer in one call, so the caller does not have to keep the
hazard vector and \code{wind_direction_10m} aligned by hand. Mirrors
\code{\link[=odour_risk]{odour_risk()}}.
}
\details{
This is the entry point for the common case; use \code{\link[=litter_exposure]{litter_exposure()}} directly
if you already have a hazard vector. Mapping the relative exposure onto an
operational index is a site-specific calibration step (issues #11/#8).
}
\seealso{
\code{\link[=litter_hazard]{litter_hazard()}}, \code{\link[=litter_exposure]{litter_exposure()}}, \code{\link[=odour_risk]{odour_risk()}}.
}
